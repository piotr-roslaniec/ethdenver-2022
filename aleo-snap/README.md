# `aleo-snap`

## Usage

Build deps:

```bash
cd ../aleo-wasm-bundler
yarn install --check-files
yarn build:clean
```

No w build this project:

```bash
yarn install --check-files
yarn build
yarn serve
```
